# Project Context — landing-axilrent

> Generated by `sdd-init` on 2026-06-30. Read-only detection; no installs or builds performed.

## Stack

| Layer | Detected | Notes |
|---|---|---|
| Language | TypeScript (intended) | No `tsconfig.json` or `package.json` yet |
| Framework | Next.js 15 App Router (intended) | Inferred from skill registry (`nextjs-15`) |
| Runtime | Node.js (intended) | Not installed |
| UI Library | React 19 (intended) | Inferred from skill registry (`react-19`) |
| Styling | Tailwind CSS 4 (intended) | Inferred from skill registry (`tailwind-4`) + `DESIGN.md` tokens |
| State | Zustand 5 (intended) | Inferred from skill registry |
| Validation | Zod 4 (intended) | Inferred from skill registry |
| Package manager | Undetermined | No lockfile or manifest present |
| Build tool | Undetermined | Depends on Next.js selection |

## Conventions

- **Design system**: `DESIGN.md` defines colors (Axil Red `#b7102c`, Financial Green `#006d43`, Dark Navy `#1D3557`), typography (Hanken Grotesk headlines, Inter body), spacing (8px base, 1200px max container), shape (Rounded-XL cards, Rounded buttons), and elevation (ambient shadows, tonal layering).
- **Code style**: No linter/formatter config detected (no `.eslintrc`, `biome.json`, `.prettierrc`).
- **Naming**: No source files to observe. Follow TypeScript/React conventions by default.
- **Folder layout**: Not yet established.

## Architecture

- **Pattern**: Greenfield single-app landing page. No monorepo tooling detected.
- **Existing structure**: Only `DESIGN.md` (design tokens + component guidelines) and `.atl/` (skill registry) exist.
- **Component model**: `DESIGN.md` describes atomic components (buttons, cards, input fields, chips/badges, data lists) — suggests atomic design or component-library approach.
- **Codegraph index**: Not present.

## Testing

| Capability | Status |
|---|---|
| Test runner | Not configured |
| Test scripts | None (`package.json` missing) |
| Coverage tool | Not configured |
| E2E framework | Not configured (Playwright skill available but unused) |
| CI config | None detected |

## Strict TDD

**`strict_tdd: false`**

No test runner, no test scripts, no `package.json`. A test-first loop cannot run until the project is scaffolded with at minimum a framework, a test runner (e.g. Vitest for unit, Playwright for E2E), and corresponding npm scripts.

## Observations

- **Greenfield**: Zero source code. One commit (`a89288b`) containing only `DESIGN.md` and `.atl/` skill registry.
- **Git**: Initialized, remote at `https://github.com/Cosifran/landing-axilrent.git`.
- **OpenSpec**: Did not exist prior to this init; `openspec/project.md` created now.
- **Skill registry**: Pre-built at `.atl/skill-registry.md` with 26 skills indexed. Relevant for this project: `nextjs-15`, `react-19`, `tailwind-4`, `typescript`, `zustand-5`, `zod-4`, `playwright`, `clean-code`.
- **Design system**: Comprehensive `DESIGN.md` ready to be translated into Tailwind tokens and React components.
- **No lockfile**: No `package-lock.json`, `pnpm-lock.yaml`, or `yarn.lock` — package manager choice is open.

## Risks

1. No source code, no `package.json`, no framework installed — project is entirely unbuilt.
2. No test infrastructure — strict TDD is unavailable until scaffolding.
3. No linter/formatter config — code style conventions are undefined.
4. Package manager undecided — affects lockfile and workspace conventions.
5. Single-commit repo — no branch strategy or CI pipeline established.
